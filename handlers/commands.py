"""
Обработчики команд бота
"""
from telegram import Update
from telegram.ext import ContextTypes
from config import user_data


async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """
    Обработчик команды /start
    Инициализирует пользователя и показывает инструкции
    """
    user_id = update.effective_user.id

    # Инициализируем данные пользователя
    user_data[user_id] = {}

    await update.message.reply_text(
        '🚀 Добро пожаловать в FutureFace AI!\n\n'
        '✨ Создаю высококачественные футуристичные портреты с передовыми технологиями AI!\n\n'
        '📸 Отправь мне свое фото, и я создам потрясающий стилизованный портрет в футуристичном стиле!\n\n'
        '🎨 Готов к трансформации? Отправляй фото!'
    )
