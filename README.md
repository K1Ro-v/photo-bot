# Telegram Bot для обработки изображений через Seaart API

Этот бот принимает изображения от пользователей в Telegram, отправляет их в n8n для обработки через Seaart API и возвращает результат обратно пользователю.

## 🚀 Возможности

- ✅ Приём фотографий от пользователей
- ✅ Отправка изображений в n8n webhook
- ✅ Обработка через Seaart API
- ✅ Возврат результата пользователю
- ✅ Логирование всех операций

## 📋 Требования

- Python 3.9+
- Telegram Bot Token (от @BotFather)
- n8n instance с настроенным webhook
- Аккаунт на Render.com

## 🛠 Установка локально

1. Клонируйте репозиторий:
```bash
git clone <your-repo-url>
cd <your-repo-name>
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` из примера:
```bash
cp .env.example .env
```

5. Отредактируйте `.env` и добавьте свои данные:
```
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
N8N_WEBHOOK_URL=https://ваш-n8n.com/webhook/process-image
```

6. Запустите бота:
```bash
python bot.py
```

## 🌐 Деплой на Render.com

### Шаг 1: Создание Telegram бота

1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и сохраните полученный токен

### Шаг 2: Настройка n8n

Создайте workflow в n8n со следующей структурой:

1. **Webhook Node** (триггер)
   - Method: POST
   - Path: `/process-image`
   - Response Mode: "When Last Node Finishes"

2. **Extract Binary Data** (если нужно)
   - Получите изображение из поля `photo`

3. **HTTP Request Node** (для Seaart API)
   - URL: Seaart API endpoint
   - Method: POST
   - Body: изображение + параметры обработки

4. **Response Node**
   - Верните обработанное изображение как:
     - `{ "image_url": "https://..." }` или
     - `{ "image_data": "base64..." }`

### Шаг 3: Деплой на Render

1. Создайте репозиторий на GitHub и загрузите код

2. Перейдите на [Render.com](https://render.com) и войдите

3. Нажмите **"New +"** → **"Background Worker"**

4. Подключите ваш GitHub репозиторий

5. Настройте параметры:
   - **Name**: telegram-seaart-bot (или любое другое)
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python bot.py`

6. Добавьте Environment Variables:
   - `TELEGRAM_BOT_TOKEN` = ваш токен
   - `N8N_WEBHOOK_URL` = URL вашего n8n webhook

7. Нажмите **"Create Background Worker"**

8. Дождитесь завершения деплоя (обычно 2-3 минуты)

## 📱 Использование бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start` для начала работы
3. Отправьте любое фото
4. Дождитесь обработки
5. Получите результат!

## 🔧 Структура проекта

```
.
├── bot.py              # Основной файл бота
├── requirements.txt    # Зависимости Python
├── .env.example       # Пример конфигурации
└── README.md          # Документация
```

## 📝 Формат ответа от n8n

Ваш n8n workflow должен возвращать JSON в одном из форматов:

**Вариант 1: URL изображения**
```json
{
  "image_url": "https://example.com/processed-image.jpg"
}
```

**Вариант 2: Base64 данные**
```json
{
  "image_data": "data:image/jpeg;base64,/9j/4AAQSkZJRg..."
}
```

**Вариант 3: Простое сообщение**
```json
{
  "message": "Изображение обработано успешно"
}
```

## 🐛 Отладка

### Логи на Render

1. Откройте ваш Background Worker на Render
2. Перейдите на вкладку **"Logs"**
3. Наблюдайте за работой бота в реальном времени

### Локальная отладка

```bash
python bot.py
```

Все логи будут выводиться в консоль.

## ⚠️ Важные замечания

1. **Таймауты**: Обработка изображения может занять до 5 минут (настраивается в коде)
2. **Размер файлов**: Telegram ограничивает размер фото до 10 МБ при сжатии
3. **Rate limits**: Убедитесь, что ваш Seaart API не имеет слишком строгих лимитов

## 🔒 Безопасность

- Никогда не коммитьте файл `.env` в репозиторий
- Используйте Environment Variables на Render
- Регулярно обновляйте зависимости

## 📊 Мониторинг

На Render.com вы можете отслеживать:
- Статус работы бота
- Использование ресурсов
- Логи в реальном времени
- Количество перезапусков

## 🆘 Поддержка

Если у вас возникли проблемы:

1. Проверьте логи на Render
2. Убедитесь, что Environment Variables установлены правильно
3. Проверьте, что n8n webhook доступен и работает
4. Убедитесь, что токен бота валидный

## 📄 Лицензия

MIT License

---

Создано для обработки изображений через Seaart API с использованием n8n и Telegram Bot API.
