# Seedream Bot - Telegram Image Generation Bot

Telegram бот для генерации изображений через n8n с использованием Seedream AI.

## Структура проекта

```
seedream-bot/
├── bot.py                      # Главный файл запуска бота
├── config.py                   # Конфигурация и глобальные переменные
├── handlers/                   # Обработчики Telegram событий
│   ├── __init__.py
│   ├── commands.py            # Обработчик команды /start
│   ├── callbacks.py           # Обработчики кнопок (зарезервировано)
│   └── messages.py            # Обработчики текстовых и фото сообщений
├── services/                   # Бизнес-логика и сервисы
│   ├── __init__.py
│   ├── n8n_service.py         # Работа с n8n API
│   └── request_manager.py     # Управление активными запросами
└── ui/                         # Пользовательский интерфейс
    ├── __init__.py
    └── keyboards.py            # Telegram клавиатуры (зарезервировано)

```

## Модули

### `config.py`
Конфигурация бота:
- Токены и URL
- ThreadPoolExecutor (20 параллельных запросов)
- Хранилище данных пользователей

### `handlers/`
Обработчики Telegram событий:
- **commands.py** - команда `/start` и инициализация пользователя
- **callbacks.py** - обработчики callback-кнопок (зарезервировано для будущих расширений)
- **messages.py** - обработка текстовых промптов и фото

### `services/`
Бизнес-логика:
- **n8n_service.py** - отправка запросов в n8n, асинхронная обработка, отправка результатов
- **request_manager.py** - блокировка/разблокировка пользователей для предотвращения множественных запросов

### `ui/`
Пользовательский интерфейс:
- **keyboards.py** - Telegram inline клавиатуры (зарезервировано для будущих расширений)

## Возможности

- ⚡ **Параллельная обработка**: до 20 одновременных запросов от разных пользователей
- 🔒 **Блокировка на пользователя**: каждый пользователь может иметь только 1 активный запрос
- 🚀 **Асинхронная архитектура**: неблокирующая обработка запросов
- 🎨 **Простой интерфейс**: отправьте промпт - получите изображение
- 📷 **Поддержка изображений**: генерация изображений на основе загруженного фото с промптом

## Использование

Для генерации изображения:
1. Отправьте текстовое сообщение с описанием желаемого изображения
2. Или отправьте фото с описанием (caption) для генерации на основе изображения

## Запуск

```bash
# Установка зависимостей
pip install python-telegram-bot requests

# Установка переменных окружения
export BOT_TOKEN="your_telegram_bot_token"
export N8N_WEBHOOK_URL="your_n8n_webhook_url"

# Запуск бота
python bot.py
```

## Переменные окружения

- `BOT_TOKEN` - токен Telegram бота (от @BotFather)
- `N8N_WEBHOOK_URL` - URL webhook для n8n

## Логирование

Бот выводит подробные логи для отслеживания:
- 🔐 Блокировка пользователя
- 🔓 Разблокировка пользователя
- 🔒 Попытка отправки при активном запросе
- 📤 Отправка запроса в n8n
- ✅ Успешный ответ от n8n
- ❌ Ошибки обработки
